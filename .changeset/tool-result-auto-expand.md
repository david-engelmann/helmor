---
"helmor": patch
---

Auto-expand short tool-result blocks in the chat panel so the agent's output (file listings, command stdout, hostname checks) is visible at a glance instead of hidden behind an extra click; long results (>1500 chars or >40 lines) still collapse by default and the chevron lets you open them.
